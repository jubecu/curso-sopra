spring:
  application:
    name: ejercicioPeliculasRestJPA
  datasource:
    url: jdbc:mysql://mysql-server:3306/mysql
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
eureka:
  client:
    service-url:
     defaultZone: http://eureka-server:8761/eureka/  